# Um cabeçalho tem vários tipos, podendo ser um cabeçalho para organização e documentação, um cabeçalho HTTP para comunicação de dados ou um cabeçalho de ficheiros que contém metadados e informações de ficheiro, já as docstring são um tipo específico de stirng literal utilziada no Python para documentar funções, classes ou módulos, aparecendo em aspas triplas..

# adicione um cabeçalho padrão neste arquivo e resolva os exercícios, não esqueça de colocar as docstrings padrão...


def e_par(n: int) -> bool:
    """Retorna True se n é par, senão False."""
    # TODO: retorne se n é par
    ...

def fatorial(n: int) -> int:
    """Retorna n! (n fatorial). Para n<0, levante ValueError."""
    # TODO: implemente de forma iterativa (sem recursão)
    ...

def maximo(nums):
    """Retorna o maior elemento de uma lista não vazia, sem usar max()."""
    # TODO: percorra a lista guardando o maior atual
    ...
